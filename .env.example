# Server configuration
PORT=3000
NODE_ENV=development

# Production allowed CORS origins (comma-separated list)
# Defaults to '*' if not set
ALLOWED_ORIGINS=*

# AI Provider configurations
# Supported providers: mock, openai, gemini, openrouter
AI_PROVIDER=mock

# AI API Keys
OPENAI_API_KEY=
GEMINI_API_KEY=
OPENROUTER_API_KEY=

# OpenRouter configuration
OPENROUTER_MODEL=google/gemini-2.5-flash
OPENROUTER_MAX_TOKENS=512

# Alexa configuration
# Set to 'true' to bypass cryptographic signature verification during local testing/mocking
SKIP_ALEXA_VERIFICATION=false
